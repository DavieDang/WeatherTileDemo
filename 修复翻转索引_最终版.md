# 修复 Y 轴翻转导致的索引错误

## 🎯 问题根源

通过日志对比发现：

```
[DEBUG] Row 8 (原始位置):
  [8,0] = 0xFF00A09E (R=0 G=160 B=158)

[DEBUG] 输出瓦片:
  Output[0,0] = 0xFF00778A (R=0 G=119 B=138)
```

**像素值不匹配！** 说明读取的不是 row 8 的数据。

### 原因分析

因为添加了 Y 轴翻转：

```objc
CGContextTranslateCTM(bitmapContext, 0, height);
CGContextScaleCTM(bitmapContext, 1.0, -1.0);
```

这导致：
- `pixels[0]` 现在对应**原始 JPEG 的最后一行**（row 264）
- `pixels[width * 264]` 对应**原始 JPEG 的第一行**（row 0）

但之前的代码错误地认为：
- `pixels[0]` 对应 row 0
- `pixels[width * 8]` 对应 row 8 ❌ 错误！

## ✅ 修复方案

### 正确的索引计算

```objc
// 错误的（之前）：
uint32_t srcPixel = pixels[(y + 8) * width + x];

// 正确的（现在）：
NSInteger srcRow = height - 8 - y - 1;
uint32_t srcPixel = pixels[srcRow * width + x];
```

### 计算逻辑

| 输出行 y | srcRow 计算 | srcRow 值 | 对应原始 JPEG |
|---------|------------|----------|--------------|
| 0 | 265 - 8 - 0 - 1 | 256 | row 8（数据起始） |
| 1 | 265 - 8 - 1 - 1 | 255 | row 9 |
| ... | ... | ... | ... |
| 255 | 265 - 8 - 255 - 1 | 1 | row 263（数据末尾） |

这样：
- 输出瓦片的第一行 = 原始 JPEG 的 row 8
- 输出瓦片的最后一行 = 原始 JPEG 的 row 263
- **跳过了顶部 8 行（包括色块条）** ✅

## 📊 数据流（修复后）

```
原始 JPEG (257x265)
├── Row 0-7:   色块条+头部 ━━━━━━━━━━━━━━━━━━ ❌ 跳过
├── Row 8:     数据起始 ━━━━━━━━━━━━━━━━━━━━ ✅ 输出 y=0
├── Row 9-263: 数据 ━━━━━━━━━━━━━━━━━━━━━━━ ✅ 输出 y=1~255
└── Row 264:   最后一行 ━━━━━━━━━━━━━━━━━━━ ❌ 跳过

         ↓ Y 轴翻转后

像素缓冲区 pixels[]
├── pixels[0]:           Row 264 ━━━━━━━━━━━━ ❌ 跳过
├── pixels[width * 1]:   Row 263 ━━━━━━━━━━━━ ✅ srcRow=1
├── ...
├── pixels[width * 256]: Row 8 ━━━━━━━━━━━━━━ ✅ srcRow=256
└── pixels[width * 264]: Row 0 ━━━━━━━━━━━━━━ ❌ 跳过

         ↓ 输出

PNG (256x256)
├── Output[0]:   来自 srcRow=256 (原始 row 8)  ✅
├── Output[1]:   来自 srcRow=255 (原始 row 9)  ✅
├── ...
└── Output[255]: 来自 srcRow=1   (原始 row 263) ✅
```

## 🚀 验证步骤

### 1. 重新编译运行

```bash
# Clean Build Folder (Shift+Command+K)
# Run (Command+R)
```

### 2. 查看日志

应该看到：

```
[DEBUG] ✂️ 裁剪计算：height=265, 读取 srcRow 范围: 256 ~ 1
[DEBUG] 期望: srcRow=256 对应原始 row 8, srcRow=1 对应原始 row 263

[DEBUG] Row 8 (data should start here):
  [8,0] = 0xFF00A09E (R=0 G=160 B=158)

[DEBUG] 输出瓦片前5个像素:
  Output[0,0] = 0xFF00A09E (R=0 G=160 B=158)  ← 应该匹配！
```

**关键**：Row 8 的像素 = Output[0,0] 的像素 ✅

### 3. 视觉验证

地图上应该显示：
- ✅ **没有顶部色块条**
- ✅ **原始 JPEG 的蓝绿色调**
- ✅ **256x256 干净瓦片**

## 🎓 经验教训

### Y 轴翻转的影响

当使用 `CGContextScaleCTM(bitmapContext, 1.0, -1.0)` 翻转 Y 轴时：

1. **像素缓冲区的顺序反转**
   - `pixels[0]` 不再是顶部，而是底部
   - 需要相应地调整索引计算

2. **正确的索引公式**
   - 要读取原始图像的 row R
   - 应该从 `pixels[(height - R - 1) * width]` 读取

3. **裁剪时的考虑**
   - 跳过顶部 N 行 = 从 `srcRow = height - N - 1` 开始
   - 读取 M 行 = 到 `srcRow = height - N - M` 结束

### 调试技巧

1. **对比关键像素** - Row 8 vs Output[0,0]
2. **打印索引计算** - 确认 srcRow 的范围
3. **逐步验证** - 先确保代码执行，再验证结果

---

**修复时间**: 2026-08-13 19:00  
**修复内容**: 修正 Y 轴翻转后的索引计算  
**关键公式**: `srcRow = height - 8 - y - 1`  
**预期结果**: Row 8 像素 = Output[0,0] 像素  
**状态**: ✅ 已修复，待验证
