# 瓦片断层问题诊断

## 问题现象

瓦片排列不连续，出现断层（拼接错位）。

## 可能的原因

### 1. Y 轴仍然颠倒

之前使用 CGDataProviderCopyData 读取像素时，可能没有正确处理 Y 轴。

**验证方法**：查看调试日志中的像素值

### 2. 头部解码位置错误

头部的 28 字节位置可能不正确。

### 3. X 轴循环包裹问题

Zoom 0 时只有 1 个瓦片 (0,0)，不需要循环包裹。
但如果代码有问题，可能导致坐标错位。

---

## 需要的诊断日志

请运行 App 并提供以下信息：

1. **完整的瓦片请求日志**
   ```
   [DEBUG] ===== Tile request: z=? x=? y=? =====
   ```

2. **像素值日志**
   ```
   [DEBUG] First 5 pixels:
   [DEBUG] Row 4 (header):
   [DEBUG] Row 8 (data):
   [DEBUG] Last row:
   ```

3. **头部解码日志**
   ```
   [DEBUG] Header: rMin=? rMax=? gMin=? gMax=?
   ```

4. **断层出现的位置**
   - 是在所有瓦片边界都有断层？
   - 还是只在特定方向（上下/左右）有断层？

