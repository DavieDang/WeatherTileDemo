# 显示原始 JPEG 颜色（跳过头部色块条）

## 🎯 当前配置

**不进行风速解码和着色，直接显示原始 JPEG 的颜色**

### 修改内容

1. **WindTileServer.m**：
   - 跳过风速解码（`WindyWindTileDecoder`）
   - 跳过颜色映射（`WindSpeedColorizer`）
   - 直接从原始像素创建输出瓦片
   - 裁剪掉顶部 8 行（色块条 + 头部区域）

2. **输出规格**：
   - 输入：257x265 JPEG
   - 跳过：前 8 行（row 0-7）
   - 输出：256x256 PNG（从 row 8-263）

### 代码逻辑

```objc
// 创建256x256的输出（跳过前8行）
uint32_t *coloredPixels = (uint32_t *)malloc(256 * 256 * sizeof(uint32_t));
for (int y = 0; y < 256; y++) {
    for (int x = 0; x < 256; x++) {
        // 从源图像的第9行开始读取（跳过0-7行）
        uint32_t srcPixel = pixels[(y + 8) * width + x];
        coloredPixels[y * 256 + x] = srcPixel;
    }
}
```

## 📊 数据流

```
原始 JPEG (257x265)
├── Row 0:     色块条 ━━━━━━━━━ ❌ 跳过
├── Row 1-3:   保留区 ━━━━━━━━━ ❌ 跳过
├── Row 4:     头部元数据 ━━━━━ ❌ 跳过
├── Row 5-7:   保留区 ━━━━━━━━━ ❌ 跳过
└── Row 8-263: 风场数据 ━━━━━━━ ✅ 复制到输出

                    ↓

输出 PNG (256x256)
└── 原始 JPEG 的颜色（row 8-263）
    不经过任何解码或着色处理
```

## 🎨 预期效果

运行后，你会在地图上看到：

1. **原始的蓝绿色调** - Windy JPEG 的自然颜色
2. **没有色块条** - 顶部 8 行已被裁剪
3. **256x256 干净瓦片** - 可以无缝叠加到地图

### 与 Windy.com 对比

- ✅ **如果颜色匹配**：说明 JPEG 本身就包含了正确的可视化颜色
- ❌ **如果颜色不对**：说明需要进行风速解码和重新着色

## 🚀 运行步骤

```bash
cd /Users/admin/Desktop/AgentFile/气象升级项目/WeatherTileDemo
open WeatherTileDemo.xcworkspace
# Clean Build Folder (Shift+Command+K)
# Run (Command+R)
```

## 📝 验证清单

运行后检查：

- [ ] 程序正常启动，无崩溃
- [ ] 地图上显示风场瓦片
- [ ] 瓦片颜色是蓝绿色调（不是彩色风速图）
- [ ] 没有看到顶部的色块条
- [ ] 瓦片边缘平滑，无明显裁剪痕迹

## 🔄 后续步骤

### 如果原始颜色看起来不错

**可能的选择**：
1. **直接使用原始颜色** - 最简单，Windy JPEG 本身就是可视化的
2. **添加透明度/混合模式** - 让风场更好地叠加在底图上

### 如果原始颜色看起来不对

需要重新启用风速解码和着色：
1. 解决头部解析问题（或继续使用默认范围）
2. 验证 `WindSpeedColorizer` 的色阶
3. 对比 Windy.com 的色阶调整

## 🛠️ 恢复原始功能

如果需要恢复风速解码和着色，运行：

```bash
cp /Users/admin/Desktop/AgentFile/气象升级项目/WeatherTileDemo/WeatherTileDemo/WindTileServer.m.backup_before_original \
   /Users/admin/Desktop/AgentFile/气象升级项目/WeatherTileDemo/WeatherTileDemo/WindTileServer.m
```

然后重新编译。

---

**当前状态**: ✅ 显示原始颜色（跳过头部8行）  
**修改文件**: WindTileServer.m  
**目的**: 快速验证 JPEG 解码和裁剪是否正确  
**下一步**: 根据显示效果决定是否需要重新着色
