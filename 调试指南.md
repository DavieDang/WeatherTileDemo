# 气象瓦片颜色问题调试指南

## 🔍 问题现状

颜色显示仍然不正确，需要通过日志分析确定实际的像素格式。

## 📝 已添加的调试日志

### 1. WindTileServer.m（已有日志）
- 第 242-252 行：打印前 5 个像素的 RGBA 值
- 第 254-262 行：打印第 9 行数据区样本
- 第 265-273 行：打印第 4 行头部区域
- 第 304-308 行：打印着色后的样本像素

### 2. WindyWindTileDecoder.m（新增日志）
- 数据区：打印前 5 个像素的 RGB 提取结果
- 头部：打印头部解析结果和前 3 个头部像素

### 3. WindSpeedColorizer.m（新增日志）
- 每 256 个像素打印一次着色结果

### 4. PixelFormatTest（新增测试类）
- 独立测试 CGBitmapContext 的像素格式
- 使用已知颜色验证实际格式

## 🚀 调试步骤

### 步骤 1：添加像素格式测试

在 `AppDelegate.m` 的 `application:didFinishLaunchingWithOptions:` 方法开头添加：

```objc
#import "PixelFormatTest.h"

- (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {
    // 添加这一行
    [PixelFormatTest testPixelFormat];
    
    // ... 原有代码
}
```

### 步骤 2：编译并运行

```bash
cd /Users/admin/Desktop/AgentFile/气象升级项目/WeatherTileDemo
open WeatherTileDemo.xcworkspace
# 在 Xcode 中编译并运行
```

### 步骤 3：查看控制台输出

在 Xcode 的控制台（View → Debug Area → Activate Console）中查找：

```
==================== 像素格式测试开始 ====================
...
=== 格式判断 ===
✅ 实际格式: ???
```

## 🔬 日志分析

### 关键日志 1：像素格式测试

```
像素[0] = 0x???????? (红色(255,0,0))
  如果是 ARGB: A=? R=? G=? B=?
  如果是 RGBA: R=? G=? B=?
  如果是 BGRA: B=? G=? R=? A=?
```

**分析方法**：
- 如果 ARGB 行显示 `R=255 G=0 B=0`，则格式是 **ARGB**
- 如果 RGBA 行显示 `R=255 G=0 B=0`，则格式是 **RGBA**
- 如果 BGRA 行显示 `R=255 G=0 B=0`，则格式是 **BGRA**

### 关键日志 2：JPEG 像素

```
[DEBUG] First 5 pixels:
  [0] = 0x???????? (R=? G=? B=? A=?)
  [1] = 0x???????? (R=? G=? B=? A=?)
```

**分析方法**：
- 对比像素格式测试的结果
- 验证 RGB 提取方式是否正确

### 关键日志 3：头部解码

```
[DECODER] Header decoded: rMin=?.?? rMax=?.?? gMin=?.?? gMax=?.??
```

**期望值**：
- rMin/rMax 应该在 -50 到 +50 之间（m/s）
- gMin/gMax 应该在 -50 到 +50 之间（m/s）
- 如果是 0.00 或异常值，说明头部解码失败

### 关键日志 4：风速着色

```
[COLORIZER] Speed=?.?? -> RGB(?,?,?) enhanced(?,?,?) alpha=217
[COLORIZER] Output pixel: 0x????????
```

**分析方法**：
- 检查 RGB 值是否合理（0-255）
- 检查输出像素的格式是否与 CGBitmapContext 匹配

## 📊 可能的问题场景

### 场景 A：格式是 ARGB（原始代码正确）

**症状**：
- 像素格式测试显示：`✅ 实际格式: ARGB`
- 当前代码使用 RGBA 提取，导致错误

**解决方案**：
```bash
# 回退到原始代码
cp WeatherTileDemo/WindyWindTileDecoder.m.bak2 WeatherTileDemo/WindyWindTileDecoder.m
cp WeatherTileDemo/WindSpeedColorizer.m.backup WeatherTileDemo/WindSpeedColorizer.m
```

### 场景 B：格式是 RGBA（当前修复正确）

**症状**：
- 像素格式测试显示：`✅ 实际格式: RGBA`
- 头部解码成功（rMin/rMax 合理）
- 但颜色仍然不对

**可能原因**：
1. 输出 PNG 的像素格式与 CGBitmapContext 不匹配
2. Alpha 预乘问题
3. 颜色空间问题

### 场景 C：格式是 BGRA

**症状**：
- 像素格式测试显示：`✅ 实际格式: BGRA`

**解决方案**：
```objc
// WindyWindTileDecoder.m
uint8_t blue = (pixel >> 24) & 0xFF;   // B 在最高位
uint8_t green = (pixel >> 16) & 0xFF;  // G 在中间
uint8_t red = (pixel >> 8) & 0xFF;     // R 在次低位

// WindSpeedColorizer.m
return (enhanced[2] << 24) | (enhanced[1] << 16) | (enhanced[0] << 8) | kOverlayAlpha;
```

## 🎯 下一步行动

### 立即执行

1. ✅ 在 AppDelegate.m 中添加 `[PixelFormatTest testPixelFormat];`
2. ✅ 重新编译并运行
3. ✅ 复制完整的控制台日志
4. ✅ 根据日志分析确定实际格式

### 根据测试结果

- **如果是 ARGB**：回退修复，使用原始代码
- **如果是 RGBA**：检查其他问题（Alpha 预乘、颜色空间）
- **如果是 BGRA**：应用场景 C 的修复方案

## 📋 日志收集清单

请提供以下日志片段：

- [ ] `==================== 像素格式测试开始 ====================` 到测试结束的完整输出
- [ ] `[DEBUG] First 5 pixels:` 的输出
- [ ] `[DECODER] Header decoded:` 的输出
- [ ] `[COLORIZER] Speed=` 的输出（至少 3-5 条）
- [ ] 任何错误或警告信息

## 💡 快速诊断

根据第一条日志就能快速判断：

```
像素[0] = 0x???????? (红色(255,0,0))
```

- 如果 `0xFF0000??` → **ARGB 格式**（需要回退）
- 如果 `0xFF0000FF` → **RGBA 格式**（当前正确）
- 如果 `0x0000FFFF` → **BGRA 格式**（需要新方案）

---

**创建时间**: 2026-08-13  
**目的**: 通过详细日志分析确定正确的像素格式  
**预计时间**: 5-10 分钟完成测试和分析
