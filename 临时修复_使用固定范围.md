# 临时解决方案 - 使用固定的风速范围

## 🎯 当前问题

无论如何调整，头部解析出的 float 值都是 0 或不合理的值。

可能的原因：
1. Windy 更改了瓦片格式
2. 下载的 JPEG 不是原始编码格式
3. 我们对格式的理解有误

## 💡 临时解决方案

**使用固定的风速范围值**，绕过头部解析，先让颜色显示出来。

### 修改 WindyWindTileDecoder.m

在 `decodeHeader` 函数中，如果解析失败，使用默认值：

```objc
+ (Header)decodeHeader:(uint32_t *)pixels width:(NSInteger)width {
    // 尝试解析头部
    uint8_t bytes[28];
    NSInteger pixelIndex = width * 4 + 2;
    
    for (NSInteger i = 0; i < 28; i++) {
        uint32_t pixel = pixels[pixelIndex + i * 4];
        uint8_t red = (pixel >> 16) & 0xFF;
        uint8_t green = (pixel >> 8) & 0xFF;
        uint8_t blue = pixel & 0xFF;
        
        int redPacked = (int)roundf(red / 64.0f) & 0x3;
        int greenPacked = (int)roundf(green / 16.0f) & 0xF;
        int bluePacked = (int)roundf(blue / 64.0f) & 0x3;
        uint8_t packed = (redPacked << 6) | (greenPacked << 2) | bluePacked;
        bytes[i] = packed;
    }
    
    float values[7];
    memcpy(values, bytes, 28);
    
    Header header;
    header.rMin = values[0];
    header.rMax = values[1];
    header.gMin = values[2];
    header.gMax = values[3];
    
    // ⭐ 如果解析出的值不合理，使用固定的默认值
    BOOL valid = isfinite(header.rMin) && isfinite(header.rMax) && header.rMax > header.rMin &&
                 isfinite(header.gMin) && isfinite(header.gMax) && header.gMax > header.gMin;
    
    if (!valid) {
        NSLog(@"[DECODER] ⚠️ 头部解析失败，使用默认范围");
        // Windy 典型的风速范围（根据全球风速统计）
        header.rMin = -20.0f;  // U 分量最小值
        header.rMax = 20.0f;   // U 分量最大值
        header.gMin = -20.0f;  // V 分量最小值
        header.gMax = 20.0f;   // V 分量最大值
    }
    
    NSLog(@"[DECODER] Header decoded: rMin=%.2f rMax=%.2f gMin=%.2f gMax=%.2f %@",
          header.rMin, header.rMax, header.gMin, header.gMax,
          valid ? @"✅" : @"⚠️ (using defaults)");
    
    return header;
}
```

### 优点

1. **绕过头部解析问题** - 先让程序运行起来
2. **可以测试颜色映射** - 验证 WindSpeedColorizer 是否正确
3. **快速验证** - 看看风场瓦片能否正常显示

### 缺点

1. **不精确** - 固定范围可能不匹配实际数据
2. **临时方案** - 需要后续找到正确的头部格式

## 🎨 验证颜色映射

使用固定范围后，我们可以：

1. **查看颜色是否正常** - 应该显示蓝-绿-黄-红-紫的梯度
2. **对比 windy.com** - 看看颜色是否匹配
3. **调整范围** - 如果颜色太淡或太浓，调整 ±20 的范围

### Windy.com 的风速范围

根据 Windy.com 的色阶：
- 0-5 m/s: 浅蓝色
- 5-10 m/s: 绿色
- 10-15 m/s: 黄色
- 15-20 m/s: 橙色
- 20-25 m/s: 红色
- >25 m/s: 紫色

所以 ±20 m/s 是一个合理的默认范围。

## 🚀 实施步骤

1. 修改 WindyWindTileDecoder.m 添加默认值逻辑
2. 重新编译运行
3. 查看地图上的风场瓦片颜色
4. 如果颜色正确，说明：
   - ✅ 颜色映射是对的
   - ✅ 数据解码是对的  
   - ❌ 只是头部格式理解有误

---

**下一步**：如果这个方案有效，我们可以：
1. 分析真实的 Windy API 响应
2. 查看是否有其他的头部编码方式
3. 或者直接使用固定范围（如果精度要求不高）
